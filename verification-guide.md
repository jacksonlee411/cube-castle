# 🚀 Cube Castle 系统验证指南

> **更新日期**: 2025年8月1日  
> **版本**: v2.0.0-alpha.2  
> **状态**: 前端架构现代化完成

## ✅ 系统状态概览

### 🔧 环境要求验证

#### 浏览器要求
- **Chrome浏览器**: ✅ 已安装并验证 (2025-08-01)
  - 支持Playwright自动化测试
  - 支持现代Web标准和ES6+特性
  - 兼容响应式设计和移动端调试

#### 系统服务状态
- **前端服务**: ✅ http://localhost:3000 (Next.js 正常运行)
- **后端服务**: ✅ http://localhost:8080 (Go API 正常运行)
- **数据库**: ✅ 50条测试员工数据已加载
- **SWR缓存**: ✅ 30秒自动刷新机制正常工作

## 🏠 主页验证 (http://localhost:3000)

### 📊 验证步骤
1. **打开浏览器**，访问 http://localhost:3000
2. **验证核心信息**：
   - 页面标题：显示 "Cube Castle" 
   - 系统状态：显示绿色运行状态
   - 版本信息：Next.js 14.2.30, React 18.3.0, TypeScript
   - 核心模块：员工管理、组织架构、职位管理、SAM仪表板、工作流管理

### 🎯 预期结果
- ✅ 页面完全加载，无404或500错误
- ✅ 所有模块链接正常工作
- ✅ 响应式设计在不同设备显示正常

## 👥 员工管理验证 (http://localhost:3000/employees)

### 🖥️ 桌面端验证步骤

1. **访问员工页面**，URL: http://localhost:3000/employees
2. **验证页面核心功能**：
   - 页面顶部：4个统计卡片（总员工、在职、待入职、部门数）
   - 数据可视化：部门分布饼图 + 员工状态柱状图
   - 智能筛选：搜索框、部门筛选、预设方案
   - 视图切换：表格视图 ↔ 卡片视图

3. **验证SWR数据同步**：
   - 数据自动加载：页面加载时自动获取员工数据
   - 缓存机制：快速切换页面数据保持一致
   - 自动刷新：等待30秒观察数据自动刷新
   - 错误处理：关闭后端服务观察错误提示

4. **验证Radix UI组件**：
   - 下拉菜单：点击列设置按钮，验证Radix UI DropdownMenu
   - 选择器：验证部门筛选下拉选择
   - 对话框：验证模态对话框交互

## 📱 移动端验证步骤

1. **打开浏览器开发者工具** (F12)
2. **点击设备模拟图标** (通常在左上角)
3. **选择设备**：iPhone 12 Pro 或 Galaxy S21
4. **验证响应式效果**：
   - 统计卡片：4列变为1列堆叠
   - 按钮文字：部分文字在小屏幕隐藏，只显示图标
   - 筛选工具：垂直布局，更紧凑的间距
   - 卡片视图：3列变为1列，头像变小

## 🎨 视觉设计验证

1. **Workday企业风格**：
   - 主色调：专业蓝色 (#0073e6)
   - 卡片阴影：微妙的投影效果
   - 趋势指示器：绿色上升、红色下降

2. **交互体验**：
   - 悬停效果：卡片轻微上移 + 阴影增强
   - 点击反馈：按钮缩放效果
   - 加载状态：骨架屏动画

## 🔍 功能测试清单

- [ ] 统计卡片显示正确数据
- [ ] 饼图和柱状图正常渲染
- [ ] 搜索框实时筛选员工
- [ ] 部门下拉筛选工作正常
- [ ] 预设筛选方案（全部在职、技术部）
- [ ] 表格/卡片视图切换
- [ ] 员工卡片选择功能
- [ ] 响应式布局适配
- [ ] 新增员工弹窗
- [ ] 编辑/删除操作

## 💻 命令行验证

如果无法使用浏览器，可以用curl测试：

```bash
# 测试主页
curl -s http://localhost:3000 | grep -o "<title>.*</title>"

# 测试员工页面
curl -s http://localhost:3000/employees | head -20

# 测试组件页面
curl -s http://localhost:3000/test-new-components | grep -i "component"
```

## 🛠️ 开发服务器管理

```bash
# 查看服务器状态
ps aux | grep "next dev"

# 查看服务器日志
tail -f dev.log

# 停止服务器
kill $(cat dev.pid)

# 重启服务器
npm run dev
```

---

**快速验证**: 直接打开 http://localhost:3000/employees 即可看到完整的Phase 2功能效果！