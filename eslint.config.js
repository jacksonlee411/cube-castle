// Root ESLint flat config placeholder for architecture guard.
// The project uses per-package configs (see frontend/eslint.config.js).
// Architecture guard extends this file; export an empty base to avoid resolution errors.
export default [];

