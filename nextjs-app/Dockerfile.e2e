# E2E Testing Docker Environment
# 提供完整的浏览器测试环境

FROM mcr.microsoft.com/playwright:v1.54.1-jammy

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装Node.js依赖
RUN npm ci

# 复制项目文件
COPY . .

# 安装Playwright浏览器 (在镜像中已预安装)
RUN npx playwright install

# 构建Next.js应用
RUN npm run build

# 暴露端口
EXPOSE 3000

# 启动脚本
CMD ["npm", "run", "test:e2e"]