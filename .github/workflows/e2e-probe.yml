name: E2E Job Probe

on:
  workflow_dispatch: {}
  push:
    branches: [ master ]
    paths:
      - 'ci/e2e-touch/**'
      - '.github/workflows/e2e-probe.yml'

jobs:
  diag:
    name: CI diag (probe)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Print context
        run: |
          echo "event=${{ github.event_name }}"
          echo "sha=${{ github.sha }}"
          echo "ref=${{ github.ref }}"
          echo "OK"
      - name: Checkout
        uses: actions/checkout@v4
      - name: Tool versions
        run: |
          uname -a
          node -v || true
          docker --version || true
          docker compose version || true
      - name: Done
        run: echo "✅ Probe job created successfully"

