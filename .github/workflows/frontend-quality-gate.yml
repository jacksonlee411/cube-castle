name: 🏰 前端质量门禁

on:
  push:
    branches: [ main, master, develop ]
    paths: 
      - 'frontend/**'
      - '.github/workflows/frontend-quality-gate.yml'
  pull_request:
    branches: [ main, master, develop ]
    paths: 
      - 'frontend/**'
      - '.github/workflows/frontend-quality-gate.yml'

jobs:
  frontend-quality-gate:
    name: 🔒 前端架构治理和质量验证
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout代码
      uses: actions/checkout@v4
      
    - name: 设置Node.js环境
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
        
    - name: 安装依赖包
      run: |
        cd frontend
        npm ci
        
    - name: 🚨 ESLint架构治理检查
      run: |
        cd frontend
        npm run lint
      continue-on-error: false
        
    - name: 📋 TypeScript类型安全检查
      run: |
        cd frontend
        npm run typecheck
      continue-on-error: false
        
    - name: 🔍 GraphQL Schema验证
      run: |
        cd frontend
        npm run validate:schema
      continue-on-error: false
        
    - name: 📏 字段命名规范验证
      run: |
        cd frontend
        npm run validate:field-naming
      continue-on-error: false
        
    - name: 🧪 契约测试完整验证
      run: |
        cd frontend
        npm run test:contract
      continue-on-error: false
        
    - name: 🏗️ 构建验证
      run: |
        cd frontend
        npm run build:verify
      continue-on-error: false
        
    - name: 📊 生成质量报告
      if: always()
      run: |
        cd frontend
        echo "📈 前端质量门禁报告" > quality-report.md
        echo "===================" >> quality-report.md
        echo "" >> quality-report.md
        echo "构建时间: $(date)" >> quality-report.md
        echo "分支: ${{ github.ref_name }}" >> quality-report.md
        echo "提交: ${{ github.sha }}" >> quality-report.md
        echo "" >> quality-report.md
        
        # ESLint结果
        echo "## ESLint检查结果" >> quality-report.md
        if npm run lint > /dev/null 2>&1; then
          echo "✅ ESLint: 通过" >> quality-report.md
        else
          echo "❌ ESLint: 失败" >> quality-report.md
        fi
        
        # 契约测试结果  
        echo "## 契约测试结果" >> quality-report.md
        if npm run test:contract > /dev/null 2>&1; then
          echo "✅ 契约测试: 32/32 通过" >> quality-report.md
        else
          echo "❌ 契约测试: 失败" >> quality-report.md
        fi
        
    - name: 上传质量报告
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: frontend-quality-report
        path: frontend/quality-report.md
        retention-days: 7

  quality-gate-result:
    name: ✅ 质量门禁结果
    needs: frontend-quality-gate
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: 检查质量门禁结果
      run: |
        if [[ "${{ needs.frontend-quality-gate.result }}" != "success" ]]; then
          echo "❌ 前端质量门禁失败 - 阻止合并"
          echo ""
          echo "🚨 发现以下问题需要修复："
          echo "1. ESLint架构违规 (直接fetch调用、alert使用等)"
          echo "2. TypeScript类型安全问题 (any类型滥用)"
          echo "3. GraphQL Schema语法错误"
          echo "4. 字段命名不符合camelCase规范"
          echo "5. 契约测试失败"
          echo "6. 构建验证失败"
          echo ""
          echo "📋 修复指南："
          echo "• 运行 'cd frontend && npm run lint' 查看详细错误"
          echo "• 运行 'cd frontend && npm run lint -- --fix' 自动修复"
          echo "• 参考 docs/development-plans/09-code-review-checklist.md"
          echo ""
          echo "🛑 代码合并被阻止，请修复所有质量问题后重新提交"
          exit 1
        else
          echo "✅ 前端质量门禁通过 - 允许合并"
          echo ""
          echo "🎉 所有质量检查已通过："
          echo "• ✅ ESLint架构治理检查"
          echo "• ✅ TypeScript类型安全检查"
          echo "• ✅ GraphQL Schema验证"
          echo "• ✅ 字段命名规范验证"
          echo "• ✅ 契约测试验证"
          echo "• ✅ 构建验证"
          echo ""
          echo "代码质量符合企业级标准，可以安全合并。"
        fi