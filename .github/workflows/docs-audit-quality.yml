name: 📚 文档与审计守护

on:
  push:
    branches: [ main, master, develop ]
    paths:
      - 'cmd/hrms-server/command/internal/audit/**'
      - 'docs/**'
      - 'scripts/quality/**'
      - 'Makefile'
      - 'package.json'
      - '.github/workflows/docs-audit-quality.yml'
  pull_request:
    branches: [ main, master, develop ]
    paths:
      - 'cmd/hrms-server/command/internal/audit/**'
      - 'docs/**'
      - 'scripts/quality/**'
      - 'Makefile'
      - 'package.json'
      - '.github/workflows/docs-audit-quality.yml'

jobs:
  lint-audit:
    name: 🔍 审计模块守护
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.22'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run audit checks
        run: npm run lint:audit

  doc-archive-check:
    name: 📁 文档归档守护
    runs-on: ubuntu-latest
    needs: lint-audit
    if: always()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate active vs archive plans
        run: npm run lint:docs
