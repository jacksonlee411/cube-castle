// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gaogu/cube-castle/go-app/ent/assignmentdetails"
	"github.com/gaogu/cube-castle/go-app/ent/positionassignment"
	"github.com/gaogu/cube-castle/go-app/ent/predicate"
	"github.com/google/uuid"
)

// AssignmentDetailsQuery is the builder for querying AssignmentDetails entities.
type AssignmentDetailsQuery struct {
	config
	ctx            *QueryContext
	order          []assignmentdetails.OrderOption
	inters         []Interceptor
	predicates     []predicate.AssignmentDetails
	withAssignment *PositionAssignmentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AssignmentDetailsQuery builder.
func (adq *AssignmentDetailsQuery) Where(ps ...predicate.AssignmentDetails) *AssignmentDetailsQuery {
	adq.predicates = append(adq.predicates, ps...)
	return adq
}

// Limit the number of records to be returned by this query.
func (adq *AssignmentDetailsQuery) Limit(limit int) *AssignmentDetailsQuery {
	adq.ctx.Limit = &limit
	return adq
}

// Offset to start from.
func (adq *AssignmentDetailsQuery) Offset(offset int) *AssignmentDetailsQuery {
	adq.ctx.Offset = &offset
	return adq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (adq *AssignmentDetailsQuery) Unique(unique bool) *AssignmentDetailsQuery {
	adq.ctx.Unique = &unique
	return adq
}

// Order specifies how the records should be ordered.
func (adq *AssignmentDetailsQuery) Order(o ...assignmentdetails.OrderOption) *AssignmentDetailsQuery {
	adq.order = append(adq.order, o...)
	return adq
}

// QueryAssignment chains the current query on the "assignment" edge.
func (adq *AssignmentDetailsQuery) QueryAssignment() *PositionAssignmentQuery {
	query := (&PositionAssignmentClient{config: adq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := adq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := adq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assignmentdetails.Table, assignmentdetails.FieldID, selector),
			sqlgraph.To(positionassignment.Table, positionassignment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, assignmentdetails.AssignmentTable, assignmentdetails.AssignmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(adq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AssignmentDetails entity from the query.
// Returns a *NotFoundError when no AssignmentDetails was found.
func (adq *AssignmentDetailsQuery) First(ctx context.Context) (*AssignmentDetails, error) {
	nodes, err := adq.Limit(1).All(setContextOp(ctx, adq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{assignmentdetails.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) FirstX(ctx context.Context) *AssignmentDetails {
	node, err := adq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AssignmentDetails ID from the query.
// Returns a *NotFoundError when no AssignmentDetails ID was found.
func (adq *AssignmentDetailsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = adq.Limit(1).IDs(setContextOp(ctx, adq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{assignmentdetails.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := adq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AssignmentDetails entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AssignmentDetails entity is found.
// Returns a *NotFoundError when no AssignmentDetails entities are found.
func (adq *AssignmentDetailsQuery) Only(ctx context.Context) (*AssignmentDetails, error) {
	nodes, err := adq.Limit(2).All(setContextOp(ctx, adq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{assignmentdetails.Label}
	default:
		return nil, &NotSingularError{assignmentdetails.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) OnlyX(ctx context.Context) *AssignmentDetails {
	node, err := adq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AssignmentDetails ID in the query.
// Returns a *NotSingularError when more than one AssignmentDetails ID is found.
// Returns a *NotFoundError when no entities are found.
func (adq *AssignmentDetailsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = adq.Limit(2).IDs(setContextOp(ctx, adq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{assignmentdetails.Label}
	default:
		err = &NotSingularError{assignmentdetails.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := adq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AssignmentDetailsSlice.
func (adq *AssignmentDetailsQuery) All(ctx context.Context) ([]*AssignmentDetails, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryAll)
	if err := adq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AssignmentDetails, *AssignmentDetailsQuery]()
	return withInterceptors[[]*AssignmentDetails](ctx, adq, qr, adq.inters)
}

// AllX is like All, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) AllX(ctx context.Context) []*AssignmentDetails {
	nodes, err := adq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AssignmentDetails IDs.
func (adq *AssignmentDetailsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if adq.ctx.Unique == nil && adq.path != nil {
		adq.Unique(true)
	}
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryIDs)
	if err = adq.Select(assignmentdetails.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := adq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (adq *AssignmentDetailsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryCount)
	if err := adq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, adq, querierCount[*AssignmentDetailsQuery](), adq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) CountX(ctx context.Context) int {
	count, err := adq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (adq *AssignmentDetailsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryExist)
	switch _, err := adq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (adq *AssignmentDetailsQuery) ExistX(ctx context.Context) bool {
	exist, err := adq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AssignmentDetailsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (adq *AssignmentDetailsQuery) Clone() *AssignmentDetailsQuery {
	if adq == nil {
		return nil
	}
	return &AssignmentDetailsQuery{
		config:         adq.config,
		ctx:            adq.ctx.Clone(),
		order:          append([]assignmentdetails.OrderOption{}, adq.order...),
		inters:         append([]Interceptor{}, adq.inters...),
		predicates:     append([]predicate.AssignmentDetails{}, adq.predicates...),
		withAssignment: adq.withAssignment.Clone(),
		// clone intermediate query.
		sql:  adq.sql.Clone(),
		path: adq.path,
	}
}

// WithAssignment tells the query-builder to eager-load the nodes that are connected to
// the "assignment" edge. The optional arguments are used to configure the query builder of the edge.
func (adq *AssignmentDetailsQuery) WithAssignment(opts ...func(*PositionAssignmentQuery)) *AssignmentDetailsQuery {
	query := (&PositionAssignmentClient{config: adq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	adq.withAssignment = query
	return adq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AssignmentID uuid.UUID `json:"assignment_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AssignmentDetails.Query().
//		GroupBy(assignmentdetails.FieldAssignmentID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (adq *AssignmentDetailsQuery) GroupBy(field string, fields ...string) *AssignmentDetailsGroupBy {
	adq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AssignmentDetailsGroupBy{build: adq}
	grbuild.flds = &adq.ctx.Fields
	grbuild.label = assignmentdetails.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AssignmentID uuid.UUID `json:"assignment_id,omitempty"`
//	}
//
//	client.AssignmentDetails.Query().
//		Select(assignmentdetails.FieldAssignmentID).
//		Scan(ctx, &v)
func (adq *AssignmentDetailsQuery) Select(fields ...string) *AssignmentDetailsSelect {
	adq.ctx.Fields = append(adq.ctx.Fields, fields...)
	sbuild := &AssignmentDetailsSelect{AssignmentDetailsQuery: adq}
	sbuild.label = assignmentdetails.Label
	sbuild.flds, sbuild.scan = &adq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AssignmentDetailsSelect configured with the given aggregations.
func (adq *AssignmentDetailsQuery) Aggregate(fns ...AggregateFunc) *AssignmentDetailsSelect {
	return adq.Select().Aggregate(fns...)
}

func (adq *AssignmentDetailsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range adq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, adq); err != nil {
				return err
			}
		}
	}
	for _, f := range adq.ctx.Fields {
		if !assignmentdetails.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if adq.path != nil {
		prev, err := adq.path(ctx)
		if err != nil {
			return err
		}
		adq.sql = prev
	}
	return nil
}

func (adq *AssignmentDetailsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AssignmentDetails, error) {
	var (
		nodes       = []*AssignmentDetails{}
		_spec       = adq.querySpec()
		loadedTypes = [1]bool{
			adq.withAssignment != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AssignmentDetails).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AssignmentDetails{config: adq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, adq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := adq.withAssignment; query != nil {
		if err := adq.loadAssignment(ctx, query, nodes, nil,
			func(n *AssignmentDetails, e *PositionAssignment) { n.Edges.Assignment = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (adq *AssignmentDetailsQuery) loadAssignment(ctx context.Context, query *PositionAssignmentQuery, nodes []*AssignmentDetails, init func(*AssignmentDetails), assign func(*AssignmentDetails, *PositionAssignment)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*AssignmentDetails)
	for i := range nodes {
		fk := nodes[i].AssignmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(positionassignment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "assignment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (adq *AssignmentDetailsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := adq.querySpec()
	_spec.Node.Columns = adq.ctx.Fields
	if len(adq.ctx.Fields) > 0 {
		_spec.Unique = adq.ctx.Unique != nil && *adq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, adq.driver, _spec)
}

func (adq *AssignmentDetailsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(assignmentdetails.Table, assignmentdetails.Columns, sqlgraph.NewFieldSpec(assignmentdetails.FieldID, field.TypeUUID))
	_spec.From = adq.sql
	if unique := adq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if adq.path != nil {
		_spec.Unique = true
	}
	if fields := adq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, assignmentdetails.FieldID)
		for i := range fields {
			if fields[i] != assignmentdetails.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if adq.withAssignment != nil {
			_spec.Node.AddColumnOnce(assignmentdetails.FieldAssignmentID)
		}
	}
	if ps := adq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := adq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := adq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := adq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (adq *AssignmentDetailsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(adq.driver.Dialect())
	t1 := builder.Table(assignmentdetails.Table)
	columns := adq.ctx.Fields
	if len(columns) == 0 {
		columns = assignmentdetails.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if adq.sql != nil {
		selector = adq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if adq.ctx.Unique != nil && *adq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range adq.predicates {
		p(selector)
	}
	for _, p := range adq.order {
		p(selector)
	}
	if offset := adq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := adq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AssignmentDetailsGroupBy is the group-by builder for AssignmentDetails entities.
type AssignmentDetailsGroupBy struct {
	selector
	build *AssignmentDetailsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (adgb *AssignmentDetailsGroupBy) Aggregate(fns ...AggregateFunc) *AssignmentDetailsGroupBy {
	adgb.fns = append(adgb.fns, fns...)
	return adgb
}

// Scan applies the selector query and scans the result into the given value.
func (adgb *AssignmentDetailsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, adgb.build.ctx, ent.OpQueryGroupBy)
	if err := adgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssignmentDetailsQuery, *AssignmentDetailsGroupBy](ctx, adgb.build, adgb, adgb.build.inters, v)
}

func (adgb *AssignmentDetailsGroupBy) sqlScan(ctx context.Context, root *AssignmentDetailsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(adgb.fns))
	for _, fn := range adgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*adgb.flds)+len(adgb.fns))
		for _, f := range *adgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*adgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := adgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AssignmentDetailsSelect is the builder for selecting fields of AssignmentDetails entities.
type AssignmentDetailsSelect struct {
	*AssignmentDetailsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ads *AssignmentDetailsSelect) Aggregate(fns ...AggregateFunc) *AssignmentDetailsSelect {
	ads.fns = append(ads.fns, fns...)
	return ads
}

// Scan applies the selector query and scans the result into the given value.
func (ads *AssignmentDetailsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ads.ctx, ent.OpQuerySelect)
	if err := ads.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssignmentDetailsQuery, *AssignmentDetailsSelect](ctx, ads.AssignmentDetailsQuery, ads, ads.inters, v)
}

func (ads *AssignmentDetailsSelect) sqlScan(ctx context.Context, root *AssignmentDetailsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ads.fns))
	for _, fn := range ads.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ads.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ads.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
