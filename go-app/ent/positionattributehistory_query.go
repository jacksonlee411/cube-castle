// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gaogu/cube-castle/go-app/ent/position"
	"github.com/gaogu/cube-castle/go-app/ent/positionattributehistory"
	"github.com/gaogu/cube-castle/go-app/ent/predicate"
	"github.com/google/uuid"
)

// PositionAttributeHistoryQuery is the builder for querying PositionAttributeHistory entities.
type PositionAttributeHistoryQuery struct {
	config
	ctx          *QueryContext
	order        []positionattributehistory.OrderOption
	inters       []Interceptor
	predicates   []predicate.PositionAttributeHistory
	withPosition *PositionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PositionAttributeHistoryQuery builder.
func (pahq *PositionAttributeHistoryQuery) Where(ps ...predicate.PositionAttributeHistory) *PositionAttributeHistoryQuery {
	pahq.predicates = append(pahq.predicates, ps...)
	return pahq
}

// Limit the number of records to be returned by this query.
func (pahq *PositionAttributeHistoryQuery) Limit(limit int) *PositionAttributeHistoryQuery {
	pahq.ctx.Limit = &limit
	return pahq
}

// Offset to start from.
func (pahq *PositionAttributeHistoryQuery) Offset(offset int) *PositionAttributeHistoryQuery {
	pahq.ctx.Offset = &offset
	return pahq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pahq *PositionAttributeHistoryQuery) Unique(unique bool) *PositionAttributeHistoryQuery {
	pahq.ctx.Unique = &unique
	return pahq
}

// Order specifies how the records should be ordered.
func (pahq *PositionAttributeHistoryQuery) Order(o ...positionattributehistory.OrderOption) *PositionAttributeHistoryQuery {
	pahq.order = append(pahq.order, o...)
	return pahq
}

// QueryPosition chains the current query on the "position" edge.
func (pahq *PositionAttributeHistoryQuery) QueryPosition() *PositionQuery {
	query := (&PositionClient{config: pahq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pahq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pahq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(positionattributehistory.Table, positionattributehistory.FieldID, selector),
			sqlgraph.To(position.Table, position.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, positionattributehistory.PositionTable, positionattributehistory.PositionColumn),
		)
		fromU = sqlgraph.SetNeighbors(pahq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PositionAttributeHistory entity from the query.
// Returns a *NotFoundError when no PositionAttributeHistory was found.
func (pahq *PositionAttributeHistoryQuery) First(ctx context.Context) (*PositionAttributeHistory, error) {
	nodes, err := pahq.Limit(1).All(setContextOp(ctx, pahq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{positionattributehistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) FirstX(ctx context.Context) *PositionAttributeHistory {
	node, err := pahq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PositionAttributeHistory ID from the query.
// Returns a *NotFoundError when no PositionAttributeHistory ID was found.
func (pahq *PositionAttributeHistoryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pahq.Limit(1).IDs(setContextOp(ctx, pahq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{positionattributehistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pahq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PositionAttributeHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PositionAttributeHistory entity is found.
// Returns a *NotFoundError when no PositionAttributeHistory entities are found.
func (pahq *PositionAttributeHistoryQuery) Only(ctx context.Context) (*PositionAttributeHistory, error) {
	nodes, err := pahq.Limit(2).All(setContextOp(ctx, pahq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{positionattributehistory.Label}
	default:
		return nil, &NotSingularError{positionattributehistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) OnlyX(ctx context.Context) *PositionAttributeHistory {
	node, err := pahq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PositionAttributeHistory ID in the query.
// Returns a *NotSingularError when more than one PositionAttributeHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (pahq *PositionAttributeHistoryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pahq.Limit(2).IDs(setContextOp(ctx, pahq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{positionattributehistory.Label}
	default:
		err = &NotSingularError{positionattributehistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pahq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PositionAttributeHistories.
func (pahq *PositionAttributeHistoryQuery) All(ctx context.Context) ([]*PositionAttributeHistory, error) {
	ctx = setContextOp(ctx, pahq.ctx, ent.OpQueryAll)
	if err := pahq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PositionAttributeHistory, *PositionAttributeHistoryQuery]()
	return withInterceptors[[]*PositionAttributeHistory](ctx, pahq, qr, pahq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) AllX(ctx context.Context) []*PositionAttributeHistory {
	nodes, err := pahq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PositionAttributeHistory IDs.
func (pahq *PositionAttributeHistoryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pahq.ctx.Unique == nil && pahq.path != nil {
		pahq.Unique(true)
	}
	ctx = setContextOp(ctx, pahq.ctx, ent.OpQueryIDs)
	if err = pahq.Select(positionattributehistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pahq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pahq *PositionAttributeHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pahq.ctx, ent.OpQueryCount)
	if err := pahq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pahq, querierCount[*PositionAttributeHistoryQuery](), pahq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) CountX(ctx context.Context) int {
	count, err := pahq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pahq *PositionAttributeHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pahq.ctx, ent.OpQueryExist)
	switch _, err := pahq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pahq *PositionAttributeHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := pahq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PositionAttributeHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pahq *PositionAttributeHistoryQuery) Clone() *PositionAttributeHistoryQuery {
	if pahq == nil {
		return nil
	}
	return &PositionAttributeHistoryQuery{
		config:       pahq.config,
		ctx:          pahq.ctx.Clone(),
		order:        append([]positionattributehistory.OrderOption{}, pahq.order...),
		inters:       append([]Interceptor{}, pahq.inters...),
		predicates:   append([]predicate.PositionAttributeHistory{}, pahq.predicates...),
		withPosition: pahq.withPosition.Clone(),
		// clone intermediate query.
		sql:  pahq.sql.Clone(),
		path: pahq.path,
	}
}

// WithPosition tells the query-builder to eager-load the nodes that are connected to
// the "position" edge. The optional arguments are used to configure the query builder of the edge.
func (pahq *PositionAttributeHistoryQuery) WithPosition(opts ...func(*PositionQuery)) *PositionAttributeHistoryQuery {
	query := (&PositionClient{config: pahq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pahq.withPosition = query
	return pahq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PositionAttributeHistory.Query().
//		GroupBy(positionattributehistory.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pahq *PositionAttributeHistoryQuery) GroupBy(field string, fields ...string) *PositionAttributeHistoryGroupBy {
	pahq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PositionAttributeHistoryGroupBy{build: pahq}
	grbuild.flds = &pahq.ctx.Fields
	grbuild.label = positionattributehistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.PositionAttributeHistory.Query().
//		Select(positionattributehistory.FieldTenantID).
//		Scan(ctx, &v)
func (pahq *PositionAttributeHistoryQuery) Select(fields ...string) *PositionAttributeHistorySelect {
	pahq.ctx.Fields = append(pahq.ctx.Fields, fields...)
	sbuild := &PositionAttributeHistorySelect{PositionAttributeHistoryQuery: pahq}
	sbuild.label = positionattributehistory.Label
	sbuild.flds, sbuild.scan = &pahq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PositionAttributeHistorySelect configured with the given aggregations.
func (pahq *PositionAttributeHistoryQuery) Aggregate(fns ...AggregateFunc) *PositionAttributeHistorySelect {
	return pahq.Select().Aggregate(fns...)
}

func (pahq *PositionAttributeHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pahq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pahq); err != nil {
				return err
			}
		}
	}
	for _, f := range pahq.ctx.Fields {
		if !positionattributehistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pahq.path != nil {
		prev, err := pahq.path(ctx)
		if err != nil {
			return err
		}
		pahq.sql = prev
	}
	return nil
}

func (pahq *PositionAttributeHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PositionAttributeHistory, error) {
	var (
		nodes       = []*PositionAttributeHistory{}
		_spec       = pahq.querySpec()
		loadedTypes = [1]bool{
			pahq.withPosition != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PositionAttributeHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PositionAttributeHistory{config: pahq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pahq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pahq.withPosition; query != nil {
		if err := pahq.loadPosition(ctx, query, nodes, nil,
			func(n *PositionAttributeHistory, e *Position) { n.Edges.Position = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pahq *PositionAttributeHistoryQuery) loadPosition(ctx context.Context, query *PositionQuery, nodes []*PositionAttributeHistory, init func(*PositionAttributeHistory), assign func(*PositionAttributeHistory, *Position)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*PositionAttributeHistory)
	for i := range nodes {
		fk := nodes[i].PositionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(position.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "position_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pahq *PositionAttributeHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pahq.querySpec()
	_spec.Node.Columns = pahq.ctx.Fields
	if len(pahq.ctx.Fields) > 0 {
		_spec.Unique = pahq.ctx.Unique != nil && *pahq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pahq.driver, _spec)
}

func (pahq *PositionAttributeHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(positionattributehistory.Table, positionattributehistory.Columns, sqlgraph.NewFieldSpec(positionattributehistory.FieldID, field.TypeUUID))
	_spec.From = pahq.sql
	if unique := pahq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pahq.path != nil {
		_spec.Unique = true
	}
	if fields := pahq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, positionattributehistory.FieldID)
		for i := range fields {
			if fields[i] != positionattributehistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pahq.withPosition != nil {
			_spec.Node.AddColumnOnce(positionattributehistory.FieldPositionID)
		}
	}
	if ps := pahq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pahq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pahq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pahq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pahq *PositionAttributeHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pahq.driver.Dialect())
	t1 := builder.Table(positionattributehistory.Table)
	columns := pahq.ctx.Fields
	if len(columns) == 0 {
		columns = positionattributehistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pahq.sql != nil {
		selector = pahq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pahq.ctx.Unique != nil && *pahq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pahq.predicates {
		p(selector)
	}
	for _, p := range pahq.order {
		p(selector)
	}
	if offset := pahq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pahq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PositionAttributeHistoryGroupBy is the group-by builder for PositionAttributeHistory entities.
type PositionAttributeHistoryGroupBy struct {
	selector
	build *PositionAttributeHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pahgb *PositionAttributeHistoryGroupBy) Aggregate(fns ...AggregateFunc) *PositionAttributeHistoryGroupBy {
	pahgb.fns = append(pahgb.fns, fns...)
	return pahgb
}

// Scan applies the selector query and scans the result into the given value.
func (pahgb *PositionAttributeHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pahgb.build.ctx, ent.OpQueryGroupBy)
	if err := pahgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PositionAttributeHistoryQuery, *PositionAttributeHistoryGroupBy](ctx, pahgb.build, pahgb, pahgb.build.inters, v)
}

func (pahgb *PositionAttributeHistoryGroupBy) sqlScan(ctx context.Context, root *PositionAttributeHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pahgb.fns))
	for _, fn := range pahgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pahgb.flds)+len(pahgb.fns))
		for _, f := range *pahgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pahgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pahgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PositionAttributeHistorySelect is the builder for selecting fields of PositionAttributeHistory entities.
type PositionAttributeHistorySelect struct {
	*PositionAttributeHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pahs *PositionAttributeHistorySelect) Aggregate(fns ...AggregateFunc) *PositionAttributeHistorySelect {
	pahs.fns = append(pahs.fns, fns...)
	return pahs
}

// Scan applies the selector query and scans the result into the given value.
func (pahs *PositionAttributeHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pahs.ctx, ent.OpQuerySelect)
	if err := pahs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PositionAttributeHistoryQuery, *PositionAttributeHistorySelect](ctx, pahs.PositionAttributeHistoryQuery, pahs, pahs.inters, v)
}

func (pahs *PositionAttributeHistorySelect) sqlScan(ctx context.Context, root *PositionAttributeHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pahs.fns))
	for _, fn := range pahs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pahs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pahs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
